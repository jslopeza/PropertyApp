# Property Finding App in React Native

Demo App made in React Native which can search for properties

Steps

1. Clone the repo
1. run ``` npm install ```
1. run ``` npm start ```

*Make sure to check [React Native](http://facebook.github.io/react-native/) if you have everything setup*